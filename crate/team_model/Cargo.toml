[package]
name = "team_model"
version = "0.18.0"
authors = ["Azriel Hoh <mail@azriel.im>"]
edition = "2018"

[lib]
doctest = false
test = false

[dependencies]
amethyst = { git = "https://github.com/amethyst/amethyst.git", branch = "wasm", default-features = false, features = ["audio", "network", "renderer", "no-slow-safety-checks", "test-support", "wav"] }
derivative = "2.1.0"
derive_more = "0.99.5"
numeric_newtype_derive = { path = "../numeric_newtype_derive" }
serde = { version = "1.0.105", features = ["derive"] }
